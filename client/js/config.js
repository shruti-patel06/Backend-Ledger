// Update this to your deployed backend's URL before deploying the frontend.
// Local dev default assumes the API is running locally via `npm run dev` (port 3000).
const API_BASE_URL = "http://localhost:3000";
